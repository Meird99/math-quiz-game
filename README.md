# math-quiz-game
A fun and simple console-based math game written in Python, where the user has to calculate several times the number they chose.

## 🚀 Features
* Includes a scoring system that tracks correct answers and calculates them.
* Built using a 5-round loop.
* Uses stylized ANSI colors in the terminal (Red, Blue, Green).
